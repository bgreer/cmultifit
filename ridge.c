#include "header.h"


/* Given a mode wavenumber and frequency, determing which ridge it is (f-mode, p1, p2, etc) */
int detect_ridge (double k, double nu)
{
	printf("Detecting ridge at %f %f\n", k, nu);
	return -1;
}
